services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: ytsummarizer
      POSTGRES_PASSWORD: ytsummarizer
      POSTGRES_DB: ytsummarizer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
